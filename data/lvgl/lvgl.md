## Overview

LVGL is a C GUI library designed for embedded use. The Rust bindings are no_std-friendly by default.

## Notes

Configuring LVGL bindings can require header and build setup; Windows-specific compilation can present issues (missing headers, SDL deps, bindgen, etc.).
