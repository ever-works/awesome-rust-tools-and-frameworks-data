## Overview

Ribir markets itself as non-intrusive Rust GUI framework that reads/writes model objects directly.

## Notes

Macro magic can be opaque; the current example shows it may not work as expected.
