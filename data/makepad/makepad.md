## Overview

Makepad is a novel Rust GUI framework with a macro DSL. The API examples show a DSL to describe UI structure.

## Notes

Crates.io versions may not align with Git tags, which can complicate following published examples. The core essence is to translate UI from DSL into runtime widgets.
