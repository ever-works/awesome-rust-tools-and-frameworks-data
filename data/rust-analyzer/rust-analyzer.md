## Overview

rust-analyzer is a language server that provides IDE features for Rust. It runs as a server in the editor, offering go-to-definition, autocomplete, inline errors, and refactoring.

## Usage
- Install editor integration (e.g., VS Code extension)